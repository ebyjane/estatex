# Local dev: leave NEXT_PUBLIC_API_URL unset — apps/web/lib/api.ts defaults to your Nest API on port 8000.
# Override only if needed (e.g. different host/port or Vercel-style /api).
# NEXT_PUBLIC_API_URL=

# Mapbox - get free token at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=

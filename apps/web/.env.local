# Full API base including /api/v1 — must match apps/api .env PORT (default 8000)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Mapbox - get free token at https://account.mapbox.com/access-tokens/
# Add your token for live map on landing + properties map view
# Get free token at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=

/**
 * @deprecated SQLite-only India rent generator removed. Use `npm run seed:data` with PostgreSQL / Supabase.
 */
throw new Error(
  'generateIndiaRentData was SQLite-only. Use npm run seed:data with DATABASE_URL (Postgres / Supabase).',
);

/**
 * @deprecated SQLite-only bulk seed removed. Use `npm run seed:data` with PostgreSQL / Supabase.
 */
throw new Error(
  'sqliteBulk10k was SQLite-only. Use npm run seed:data with DATABASE_URL (Postgres / Supabase).',
);
